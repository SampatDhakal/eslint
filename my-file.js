const array = [
    1, 2, 3
]

const name = "bob"

// console.log(name);

function doSomeStuff(
    myArray) {
    for (let number of
        myArray) {
        console.log(number);
    }
}

// doSomeStuff(array);